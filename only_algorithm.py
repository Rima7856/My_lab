# Ввод коэффициентов
    # Пробуем получить коэффициент из аргументов командной строки
    # Если аргументов нет - выводим приглашение и ждем ввод пользователя

    # Пробуем преобразовать введенное значение в число
    # Если преобразование не удалось - выводим ошибку и повторяем ввод

"""
    Функция решения квадратного уравнения.

    Вычисляем дискриминант D квадратного уравнения a*x² + b*x + c = 0
    Если дискриминант меньше нуля:
        Возвращаем пустое множество корней
    Если дискриминант равен нулю:
       Находим единственный корень
       Возвращаем его
    Если дискриминант больше нуля:
       Находим корни
       Возвращаем их
    """

# Начало 
    # Вводим данные пользователя --- как?
    # Вычисляем корни уравнения --- как?
    # Вычисляем кол-во корней
    # Если корней нет
        # Выведем что корней нет
    # Если есть 1 корень
        # Укажем найденый корень
    # Если есть два корня
        # Выведем оба корня


# ----------------- ПРИМЕЧАНИЯ -----------------
# 1. Некоторые простые действия могут подразумевать больше одной строки кода, например:
a = get_coef(1, 'Введите коэффициент А:')
b = get_coef(2, 'Введите коэффициент B:')
c = get_coef(3, 'Введите коэффициент C:') # Вводим данные пользователя
# Обычно это ввод/вывод. Логически это одно дейсвтие, и это даже можно записать в одну строку,
# но пишем в несколько для улучшения читаемости кода.
# 2. Некоторые действия (обьявления переменных, математические действия) не являются
# Частью алгоритма. Логически они "не видны", но нужны для правильной работы машины. Пример:
sqrtD = math.sqrt(D) # <--- эта строка не будет требовать алгоритм
root = (-b + sqrtD) / (2.0*a)
# 3. Иногда, при построении логической цепочки, под одним действием подразумевается выполнение
# некоего сложного механизма. Так, например, у нас есть простое действие - вычисляем корни уравнения.
# При написании программы по алгоритму возникнет вопрос "А как?" - именно в таких случаях
# пишут подпрограммы (функции, процедуры)















































# Функция преобразования слова 'часы' в правильный падеж
#   Получаем корректное отображение времени в 12часом формате
#   Если время в 12часом формате равно 1
#     Правильный падеж - 'час'
#   Иначе если  в 12часом формате меньше 5 и больше или равно 2
#     Правильный падеж - 'часа'
#   Иначе
#     Правильный падеж - 'часов'
#
#     Возвращаем правильный падеж

# Функция преобразования слова 'минуты' в правильный падеж
    # Равенство числа минут с нулем 
    # Если число минут больше или равно 5 и меньше или равно 20
    #     Правильный падеж - 'минут'
    # Иначе
    #     Вычисляем последнюю цифру числа минут
    #     Если последняя цифра числа равно 1
    #         Правильный падеж - 'минута'
    #     Если последняя цифра числа больше или равна 2 и последняя цифра числа меньше 5
    #         Правильный падеж - 'минуты'
    #     Иначе 
    #         Правильный падеж - 'минут'
#
#     Возвращаем правильный падеж


# Функция получения времени суток
#     Если число часов меньше или равно 5 и больше или равно 0
#         Время суток - ночь, с правильным склонением (ночи)
#     Иначе если число часов меньше или равно 6 и больше или равно 11
#         Время суток - утро, с правильным склонением (утра)
#     Иначе если число часов меньше или равно 12 и больше или равно 17
#         Время суток - день, с правильным склонением (дня)
#     Иначе 
#         Время суток - вечер, с правильным склонением (вечера)


# Функция проверки ошибок
#     Проверка на количество вводимых элементов, если оно не равно 2
#         Вернуть статус ошибки(True) и ошибку
#     Если не минуты - числа и минуты не отрицательные
#         Ошибка - введены неверные данные по минутам
#     Если не часы - числа  и часы не отрицательные
#         Ошибка - введены неверные данные по часам
#     Если ошибка
#         Текст ошибки по типу ошибки
#     Вернуть статус ошибки и текст(если не ошибка , тескт - пусто)

def err(x):
    lst = x.split()
    if len(lst) != 2:
        return True, 'Введите ровно два числа (часы и минуты) через пробел'
    err_h = not (lst[0].isdigit()) or '-' in lst[0]
    err_m = not (lst[1].isdigit()) or '-' in lst[1]
    if not err_m:
        minutes = int(lst[1])
        err_m = not (0 <= minutes <= 59)
    if not err_h:
        hours = int(lst[0])
        err_h = not (0 <= hours <= 23)
    is_err = err_m or err_h
    text = ''

    if is_err:
        text = 'Введены недопустимые данные: '
        errors = []

        if err_h:
            errors.append('часы должны быть от 0 до 23')
        if err_m:
            errors.append('минуты должны быть от 0 до 59')
        text += ', '.join(errors)

    return is_err, text
def ready_text(hours, minutes):
    if hours == 0 and minutes == 0:
        return 'полночь'
    elif hours == 12 and minutes == 0:
        return 'полдень'

    padej_h = hours_case(hours)
    rovno, padej_m = minuts_case(minutes)
    new_hours, vrem_sut = times_of_day(hours)

    if rovno:
        return f'{new_hours} {padej_h} {vrem_sut} ровно'
    else:
        return f'{new_hours} {padej_h} {minutes} {padej_m} {vrem_sut}'

def main():
    print('Введите время в формате: "часы минуты"')
    print()
    input_time = input('Ваше время: ')
    err_status, text = err(input_time)
    while err_status:
        print(text)
        print('Введите повторно время в формате: "часы минуты"')
        print()
        input_time = input('Ваше время: ')
        err_status, text = err(input_time)
    hours, minutes = map(int, input_time.split())
    print(ready_text(hours, minutes))

if __name__ == '__main__':
    main()


# Алгоритм преобразования времени в текст

# Ввод данных пользователя
    # Попросить пользователя ввести часы и минуты через пробел
    # Получить строку ввода

# Проверка корректности ввода
    # Разделить строку на два числа
    # Если чисел не два:
        # Сообщить пользователю об ошибке и повторить ввод
    # Если часы не в диапазоне 0-23 или минуты не в диапазоне 0-59:
        # Сообщить пользователю об ошибке и повторить ввод

# Преобразование ввода
    # Преобразовать часы и минуты в целые числа

# Определение падежа для часов
    # Если часы равны 1 или 13, 25 и т.д. по модулю 12 → "час"
    # Если часы равны 2-4 или 14-16 и т.д. по модулю 12 → "часа"
    # Иначе → "часов"

# Определение падежа для минут
    # Если минуты равны 0 → ровно
    # Если минуты 5-20 → "минут"
    # Иначе:
        # Если последняя цифра 1 → "минута"
        # Если последняя цифра 2-4 → "минуты"
        # Иначе → "минут"

# Определение времени суток
    # Если часы 0-5 → "ночи"
    # Если часы 6-11 → "утра"
    # Если часы 12 → "дня"
    # Если часы 13-17 → "дня"
    # Если часы 18-23 → "вечера"

# Формирование текста
    # Если часы = 0 и минуты = 0 → "полночь"
    # Если часы = 12 и минуты = 0 → "полдень"
    # Если минуты = 0 → вывести "{часы} {падеж_часов} {часть_суток} ровно"
    # Иначе → вывести "{часы} {падеж_часов} {минуты} {падеж_минут} {часть_суток}"
# Вывод результата
# Конец

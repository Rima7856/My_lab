# ЛР 2 - сортировка одномерного числового массива

> Для учащихся 9 класса лицея 1580.
> 
> Предмет: Информатика и ИКТ

## Содержание

- [Цель работы](#цель-работы)
- [Указания по выполнению лабораторной работы](#указания-по-выполнению-лабораторной-работы)
  - [Общие советы](#общие-советы)
  - [Сортировка выбором (selection sort, min/max)](#сортировка-выбором-selection-sort-minmax)
  - [Сортировка пузырьком (bubble sort)](#сортировка-пузырьком-bubble-sort)
  - [Генератор псевдо-случайных чисел](#генератор-псевдо-случайных-чисел)
- [Задание](#задание)
- [Вопросы для защиты](#вопросы-для-защиты)

## Цель работы

- Освоить работу с массивами в Python.
- Изучить алгоритмы сортировки:
  - Сортировка выбором
  - Сортировка пузырьком
  - Дополнительный метод (на выбор)
- Проанализировать эффективность алгоритмов через подсчет операций.

## Указания по выполнению лабораторной работы

### Общие советы

- В Python для работы с массивами используются списки (list)
- Списки являются динамическими и не требуют ручного управления памятью
- Для индексации по списку и хранения его размера используйте целочисленные переменные
- Всегда проверяйте границы списка при работе с индексами

### Сортировка выбором (selection sort, min/max)

Алгоритм сортировки, имеющий худшее, среднее и лучшее время выполнения *O(n²)*. Затраты памяти *O(1)*.

Работает следующим образом:

1. Находится минимальный (или максимальный) элемент в массиве;
2. Найденное значение обменивается со значением первого (если ищется минимальный элемент) или последнего (если ищется максимальный элемент) **неотсортированного** элемента;
3. Сортируется оставшаяся часть массива (отсортированные элементы не рассматриваются).

### Сортировка пузырьком (bubble sort)

Алгоритм сортировки, имеющий худшее и среднее время выполнения *O(n²)*. Лучшее время выполнения *O(n)*. Затраты памяти *O(1)*.

Работает следующим образом:

1. В цикле попарно сравниваются элементы массива;
2. Если первый элемент больше второго, то их значения обмениваются между собой;
3. Цикл повторяется либо **n - 1** раз, либо пока не обнаружится, что перестановки элементов больше не требуются.

### Генератор псевдо-случайных чисел

В Python для генерации случайных чисел используется модуль `random`.

Пример:

```python
import random

# Генерация случайного числа от 1 до 12
random_number = random.randint(1, 12)

# Заполнение списка случайными числами
arr = [random.randint(0, 99) for _ in range(10)]
```

## Задание

Реализовать следующие методы сортировки массивов:

- Сортировка выбором;
- Сортировка пузырьком;
- Еще один любой адекватный метод сортировки. (имеется ввиду, что Stalin sort и Bogosort писать не надо 😞)
###### [Про Bogosort](https://ru.wikipedia.org/wiki/Bogosort)

Заполнить массив случайными элементами (целые числа, равномерное распределение от 0 до 99).

Предусмотреть два режима работы программы:
1. Демонстративный - в данном режиме продемонстрировать работу вашего приложения
2. Интерактивный - в данном режиме позволить пользователю вводить данные (размер массива/изменять массив по желанию (полностью или его части)), а так же выбирать действия для работы (вывод, сортировка, повторная сортирвока)

Для каждого метода вывести количество перестановок и сравнений.

Вывод должен быть оформлен в виде отформатированной таблицы.

Проанализировать быстродействие алгоритмов сортировки (на основе количества перестановок и сравнений).

## Вопросы для защиты

- Что такое массив в программировании и какими свойствами он обладает?
- В чем основное отличие сортировки выбором от сортировки пузырьком?
- Какова временная сложность изученных алгоритмов сортировки в худшем случае?
- Для чего используется функция random.randint(a, b) в Python?
- Почему сортировка выбором всегда выполняет одинаковое количество сравнений независимо от исходных данных?
- В каком случае сортировка пузырьком завершится быстрее (за меньшее количество проходов)?
- Как можно оптимизировать сортировку пузырьком? Что дает такая оптимизация?
- Объясните, почему алгоритмическая сложность O(n²) считается неэффективной для больших массивов?
- Какие преимущества и недостатки у сортировки вставками по сравнению с изученными методами?
- Что такое "устойчивость" алгоритма сортировки? Являются ли изученные вами алгоритмы устойчивыми?
- Почему в сортировке выбором количество перестановок всегда равно (n-1) в худшем случае?
- Как бы вы изменили алгоритмы для сортировки по убыванию вместо возрастания?
- В каких реальных ситуациях могут применяться изученные алгоритмы сортировки, несмотря на их невысокую эффективность?
